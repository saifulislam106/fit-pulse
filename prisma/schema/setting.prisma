model Setting {
  id        String   @id @default(cuid())
  key       String  @unique
  value     String
}

model TrainerCommissionRate {
  id        String   @id @default(cuid())
  userId    String   @unique
  rate      Float
  user      User  @relation(fields: [userId], references: [id], onDelete: Cascade)
}
